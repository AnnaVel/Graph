language: csharp
solution: GraphApplication.sln

install:
  - sudo apt-get install nunit-console
  - nuget restore GraphApplication.sln
  
script:
  - xbuild /p:Configuration="NoUnitTests" GraphApplication.sln
  - nunit-console GraphTests\bin\Debug\GraphTests.dll